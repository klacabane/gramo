'use strict';

const React = require('react');
const Library = require('./library.js');

class App extends React.Component {
  render() {
    return (
      <Library />
    );
  }
}

module.exports = App;
